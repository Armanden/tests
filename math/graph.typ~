#import "@preview/cetz:0.2.2": canvas, draw

#canvas({
  import draw: *

  // axes
  line((0,0), (5,0))
  line((0,0), (0,5))

  // plot y = x^2
  path(
    for x in range(0, 5, step: 0.1) {
      (x, x*x/5)
    }
  )
})